[package]
name = "shipsim"
version = "0.1.0"
edition = "2021"

[dependencies]
rvemu = { path = "/home/jim/projects/github.com/jimktrains/rvemu" }
bincode = {version="2.0.1", features=["serde"]}
serde = {version="1.0.219", features = ["derive"]}
